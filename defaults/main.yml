---
# defaults file for r-install role

# Primary R version to install and add to system PATH
r_version: "4.3.3"

# Alternative R version to install (optional)
r_version_alt: "4.1.3"

# Base installation directory for R
r_install_dir: "/opt/R"